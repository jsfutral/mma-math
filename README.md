# mma-math
MMA Math Calculator

Fighter identity: Sherdog fighter IDs are used as the canonical unique identifier throughout the data model. Fighter names are treated as display data only to avoid collisions.
